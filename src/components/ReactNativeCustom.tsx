import {
  View as V,
  TouchableOpacity as TO,
  TouchableHighlight as TH,
  Text as T,
  Button as B,
  FlatList as F,
} from "react-native";
import { styled } from "nativewind";

const View = styled(V);
const TouchableOpacity = styled(TO);
const TouchableHighlight = styled(TH);
const Text = styled(T);
const Button = styled(B);
const FlatList = styled(F);

export { View, TouchableOpacity, TouchableHighlight, Text, Button, FlatList };
